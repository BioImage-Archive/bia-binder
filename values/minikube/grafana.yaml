ingress:
  hosts:
  - minikube.local
  tls:
  - hosts:
    - minikube.local

persistence:
  enabled: true
  size: 100M
  storageClassName:

env:
  GF_AUTH_ANONYMOUS_ENABLED: "true"
  GF_AUTH_GITHUB_ENABLED: "false"
  GF_SERVER_DOMAIN: minikube.local
