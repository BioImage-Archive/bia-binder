hub:
  db:
    pvc:
      storageClassName:

auth:
  github:
    callbackUrl: https://localhost/vae/hub/oauth_callback

singleuser:
  storage:
    dynamic:
      storageClass:
    # extraVolumes: []
    # extraVolumeMounts: []

prePuller:
  hook:
    enabled: true
