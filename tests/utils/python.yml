kind: Pod
apiVersion: v1
metadata:
  name: ubuntu
spec:
  # Add the server as an NFS volume for the pod

  containers:
    - name: ubuntu
      image: continuumio/miniconda
      command:
        - "sleep"
        - "604800"
      resources:
        limits:
          nvidia.com/gpu: 1
