kind: Pod
apiVersion: v1
metadata:
  name: curl
  # annotations:
    # container.apparmor.security.beta.kubernetes.io/ubuntu: localhost/fuse
spec:
  # Add the server as an NFS volume for the pod
  containers:
    - name: ubuntu
      # image: ubuntu
      image: alpine/curl
      # resources:
      #   limits:
      #     smarter-devices/fuse: "1"
      #     # cpu: 1
      #     # memory: 50Mi
      #   requests:
      #     smarter-devices/fuse: "1"
          # cpu: 10m
          # memory: 50Mi
        # cpu: 10m
        # memory: 50Mi
      # securityContext: "privileged"
      #   privileged: true
      # image: docker-registry.docker-registry:5000/ubuntu
      # image: docker-registry:5000/ubuntu
      # image:  docker-registry.svc.cluster.local:5000/ubuntu
      # image: docker-registry.docker-registry.svc.cluster.local:5000/ubuntu
      # image: gcr.io/kubernetes-e2e-test-images/dnsutils:1.3
      # image: docker-registry.docker-registry.svc.cluster.local:5000/ubuntu
      command:
        # - "apt-get update -y"
        # - "apt-get install rclone fuse busybox p7zip unzip zip -y"
        # - "curl https://rclone.org/install.sh | bash"
        - "sleep"
        - "604800"
      # resources:
      #   requests:
      #     memory: "4073741824"
      #   # limits:
      #   #   nvidia.com/gpu: 1
      # securityContext:
      #   privileged: false
      #   runAsUser: 1000
      #   runAsGroup: 1000
      #   capabilities:
      #     add:
      #       - SYS_ADMIN
      # resources:
        # limits:
          # smarter-devices/fuse: 1
          # memory: 512Mi
        # requests:
        #   # smarter-devices/fuse: 1
        #   cpu: 1
        #   memory: 256Mi


# https://discourse.jupyter.org/t/storage-capacity-is-not-respected-by-hub/8080/2