apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-jupyer
  # namespace: default
spec:
  containers:
    - name: gpu-pod-jupyer
      image: cschranz/gpu-jupyter
      command:
        - "sleep"
        - "604800"
      resources:
        limits:
          nvidia.com/gpu: 1
