settings:
  kubeContext: gpu
apps:
  binderhub-prod:
    enabled: true
    valuesFiles:
    - "../binderhub/config.yaml"
    - "../binderhub/prod/config.yaml"
  docker-registry:
    enabled: true
  ingress-nginx:
    enabled: true
  jupyterhub-sandbox-prod:
    enabled: false
  jupyterhub-sandbox-dev:
    enabled: false
  cert-manager-prod:
    enabled: true
